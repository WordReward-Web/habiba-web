const ShopLoading = () => {
  return (
    <div
      style={{
        display: "flex",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      ShopLoading ...
    </div>
  );
};

export default ShopLoading;
