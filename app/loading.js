const RootLoading = () => {
  return (
    <div
      style={{
        display: "flex",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      RootLoading ...
    </div>
  );
};

export default RootLoading;
